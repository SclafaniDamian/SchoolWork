// Copyright 2022 CSCE240
// test drive our shapes
#include<rectangle.h>
using csce240_shapes::Rectangle;

/*
void doubleRectangle( Rectangle r ) 
{
  r.SetLength(r.GetLength() * 2);
  r.SetWidth(r.GetWidth() * 2);
}
*/

int main() {
//  doubleRectangle( 5 );  // without explicit, this would compile and create
//                            a Rectangle object
  return 0;
}
